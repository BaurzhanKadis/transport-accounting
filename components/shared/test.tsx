{
  /* <div className="flex justify-between items-center mb-4">
<Label className="text-sm font-medium mr-2 w-40" htmlFor="name">
  Название
</Label>
<Input
  onChange={(e) => setName(e.target.value)}
  type="text"
  placeholder="name"
  value={name}
  id="name"
/>
</div>
<div className="flex justify-between items-center mb-4">
<Label className="text-sm font-medium mr-2 w-40" htmlFor="gosnumber">
  Гос. номер
</Label>
<Input
  onChange={(e) => setGosnumber(e.target.value)}
  type="text"
  placeholder="gosnumber"
  value={gosnumber}
  id="gosnumber"
/>
</div>
<div className="flex justify-between items-center mb-4">
<Label className="text-sm font-medium mr-2 w-40" htmlFor="statusId">
  Статус
</Label>
<Input
  onChange={(e) => setStatusId(parseInt(e.target.value))}
  type="text"
  placeholder="statusId"
  value={statusId}
  id="statusId"
/>
</div>
<div className="flex justify-between items-center mb-4">
<Label className="text-sm font-medium mr-2 w-40" htmlFor="categoryId">
  Категория
</Label>
<Input
  onChange={(e) => setCategoryId(parseInt(e.target.value))}
  type="number"
  placeholder="category"
  required
  value={categoryId}
  id="categoryId"
/>
</div>
<div className="flex justify-end">
<Button onClick={() => createOrUpdateTransport()}>{title}</Button>
</div> */
}
